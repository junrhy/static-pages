{
  "name": "dental-clinic",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node set-theme.js && vite",
    "build": "node set-theme.js && vite build",
    "preview": "node set-theme.js && vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "dotenv": "^17.3.1",
    "vite": "^7.3.1"
  }
}
